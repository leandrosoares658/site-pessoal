# Portfólio Leandro Soares Pereira

Vite + React 19 com CSS por componente.

    npm install
    npm run dev

Textos: `src/data/content.js`. Ilustrações provisórias dos projetos: `src/components/ProjectVisual.jsx` (troque por prints/GIFs reais).
